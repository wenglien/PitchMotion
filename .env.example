# SpeedGun 本地開發設定
# 複製此檔案為 .env，再按需修改：
#   cp .env.example .env

# 後端 FastAPI port
BACKEND_PORT=8000

# YOLO 模型權重路徑（相對於 repo root）
YOLO_WEIGHTS=train_tool/runs/detect/baseball_yolo26n_v5/weights/best.pt
