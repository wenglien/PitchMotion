# Research

此目錄放置不會進入 App 執行路徑的研究與訓練工具。

- `vision/`：Python 電腦視覺參考實作。
- `pitch_classifier/`：球種分類模型與特徵工具。
- `yolo/`：棒球偵測資料處理、訓練腳本與模型。

請從專案根目錄執行 Python 工具。YOLO 原始資料放在 `yolo/datasets/`，訓練輸出放在 `yolo/runs/`；兩者只保留於本機，不納入 Git。
